name: test
on:
    workflow_run:
        workflows: ["build-test.yml"]
        types:
            - completed
jobs: 
  test-job:
    runs-on: ubuntu-latest
    steps: 
        - run: echo "hello world"
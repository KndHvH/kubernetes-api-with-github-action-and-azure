

name: hello-world
on: push
jobs: 
    hello-world-job:
        runs-on: ubuntu-latest
        steps:
            - name: enter repo
              uses: actions/checkout@v3
            - run: echo "$(cat test.txt)"